void main(int i) 
{
	/* search this HBA host */
#if (LINUX_VERSION_CODE >= KERNEL_VERSION(2,5,45))
	#else
for(host=scsi_hostlist;host;host=host->next){if(host->host_no==hostno){break;}}#endif


	return thislength;
}
