fc_port_t *
qla2x00_alloc_rscn_fcport(scsi_qla_host_t *ha, int flags)
{
}

void main(int i) 
{
  fc_port_t *fcport, *remote_fcport, *rscn_fcport;
  struct io_descriptor *iodesc;
  
  fc_port_t	*fcport, *new_fcport;
  
  uint16_t	index;
  
  fc_port_t	*fcport, *fcptemp;
  uint16_t	next_loopid;
  LIST_HEAD(new_fcports);

  u32 long off;

//HANDLING: test_c/bugs/59/ok/linux-2.5.65/drivers/char/ppdev.c
//parse error 
// = File "test_c/bugs/59/ok/linux-2.5.65/drivers/char/ppdev.c", line 143, characters 31
//    around = '*', whole content = 			size_t (*fn)(struct parport *, void *, size_t, int);
  int flags = 0;
  size_t (*fn)(struct parport *, void *, size_t, int);


  switch(1) {
		case USB_DT_DEVICE:
			ret = min(ctrl->wLength,
 				(u16)sizeof(struct usb_device_descriptor));

			if ((end - start) < 4)
				return NULL;
 			item->data.u32 = le32_to_cpu(get_unaligned((__le32*)start));
			start = (__u8 *)((__le32 *)start + 1);
     }


}

typedef u32 * (*decode_dirent_t)(u32 *, struct nfs_entry *, int);




