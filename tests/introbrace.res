int main() {
	if (x) {
		rc = request_threaded_irq(a,
					  b);
		rc = compat_request_threaded_irq(12);
	}
	else {
		rc = request_threaded_irq(a,
					  b);
		rc = compat_request_threaded_irq(12);
	}
}
