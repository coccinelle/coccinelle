commit 29a41d4ca3944499c3d44c49c94beef8ff781e9c
Author: Jaroslav Kysela <perex@suse.cz>
Date:   Thu Oct 7 16:28:22 2004 +0200

    [ALSA]  Fix pci_restore_state()
    
    ALSA Core
    Fixed pci_save_state() call with the new API.
    
    Signed-off-by: Takashi Iwai <tiwai@suse.de>

diff --git a/sound/core/init.c b/sound/core/init.c
index 1ff7667..2873a17 100644
--- a/sound/core/init.c
+++ b/sound/core/init.c
@@ -801,7 +801,7 @@ int snd_card_pci_resume(struct pci_dev *
 	if (card->power_state == SNDRV_CTL_POWER_D0)
 		return 0;
 	/* restore the PCI config space */
-	pci_restore_state(dev, dev->saved_config_space);
+	pci_restore_state(dev);
 	/* FIXME: correct state value? */
 	return card->pm_resume(card, 0);
 }
