#!/bin/bash
DN=`dirname $0`

if [ -z ${PYTHONPATH} ]
then
export PYTHONPATH=${DN}/python
else
export PYTHONPATH=${PYTHONPATH}:${DN}/python
fi

if [ -z ${LD_LIBRARY_PATH} ]
then
export LD_LIBRARY_PATH=${DN}/python
else
export LD_LIBRARY_PATH=${PYTHONPATH}:${DN}/python
fi

${DN}/spatch.opt $*
