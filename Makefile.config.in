# autogenerated by configure

# * for each library $1, add another entry in the same manner
MAKELIBS=@MAKE_sexplib@ @MAKE_menhirLib@ @MAKE_pycaml@ @MAKE_pcre@
LNKLIBS=@MODULES_sexplib@ @MODULES_menhirLib@ @MODULES_pycaml@ @MODULES_pcre@
OPTLNKLIBS=@MODULESOPT_sexplib@ @MODULESOPT_menhirLib@ @MODULESOPT_pycaml@ @MODULESOPT_pcre@
INCLIBS=@PATH_sexplib@ @PATH_menhirLib@ @PATH_pycaml@ @PATH_pcre@
FLAGSLIBS=@FLAGS_sexplib@ @FLAGS_menhirLib@ @FLAGS_pycaml@ @FLAGS_pcre@
# * end of library variables

SHELL?=@SHELL@
PKG_CONFIG=@PKG_CONFIG@
INSTALL=@INSTALL@

# paths
prefix?=@prefix@
exec_prefix?=@exec_prefix@
bindir?=@bindir@
datarootdir?=@datarootdir@  # share
includedir?=@includedir@    # include
libdir?=@libdir@            # lib
sysconfdir?=@sysconfdir@    # etc
mandir?=@mandir@

# C compiler
CC=@CC@
CFLAGS=@CFLAGS@
CPP=@CPP@
CPPFLAGS=@CPPFLAGS@

PYTHON_CFLAGS=@PYTHON_CFLAGS@
PYTHON_LIBS=@PYTHON_LIBS@

PCRE_CFLAGS=@PCRE_CFLAGS@
PCRE_LIBS=@PCRE_LIBS@

OCAMLFIND=@OCAMLFIND@
OCAMLBUILD=@OCAMLBUILD@
OCAMLC=@OCAMLC@
OCAMLDEP=@OCAMLDEP@
OCAMLDOC=@OCAMLDOC@
OCAMLLEX=@OCAMLLEX@
OCAMLYACC=@OCAMLYACC@
OCAMLLIB=@OCAMLLIB@
OCAMLMKLIB=@OCAMLMKLIB@
OCAMLMKTOP=@OCAMLMKTOP@
OCAMLOPT=@OCAMLOPT@
MENHIR=@MENHIR@

# pass configured commands to ocamlfind
OCAMLFIND_COMMANDS?="ocamlc=${OCAMLC} ocamlopt=${OCAMLOPT} ocamlmktop=${OCAMLMKTOP} ocamldoc=${OCAMLDOC} ocamldep=${OCAMLDOC}"

INSTALL_PROGRAM?=$(INSTALL) -m 755
INSTALL_LIB?=$(INSTALL) -m 755
INSTALL_DATA?=$(INSTALL) -m 644

# Where to install the binary
BINDIR=$bindir

# Where to install the man pages
MANDIR=$mandir

# Where to install the lib
LIBDIR=$libdir

# Where to install the configuration files
SHAREDIR=$datarootdir

BASH_COMPLETION_DIR=$(sysconfdir)/bash_completion.d

# selected libraries
FEATURE_sexplib=1
FEATURE_menhirLib=1
FEATURE_pycaml=1
FEATURE_pcre=1

# Features
FEATURE_TARZAN=@FEATURE_TARZAN@
FEATURE_PYTHON=@FEATURE_PYTHON@

# Obsolete?
PCREDIR=@PATH_pcre@
PYCAMLDIR=@PATH_pycaml@
MENHIRDIR=@PATH_menhirLib@
SEXPDIR=@PATH_sexplib@
DYNLINK=@OCAML_PKG_dynlink@

# derived vars
CLEANLIBS=$(MAKELIBS)
DEPLIBS=$(MAKELIBS)

# selected ocaml modules
PYCOCCI_FILE=@PYCOCCI_FILE@
OCAMLCOCCI_FILE=@OCAMLCOCCI_FILE@
REGEXP_FILE=@REGEXP_FILE@
